version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: techfest_platform
    restart: unless-stopped
    ports:
      - " 8000:8000\
 env_file:
 - backend/.env
 environment:
 - ENVIRONMENT=production
